# Atlas — Claude context anchor

**READ THIS FIRST, EVERY THREAD.** This file is the single source of truth for where the project is and what to do next. It is auto-loaded when a Claude thread starts in `C:\Users\riley\atlas`. Keep it accurate: **at the end of every work chunk, update the "Current status" and "NEXT ACTION" sections and append any new gotcha.** The approved foundation plan lives at `C:\Users\riley\.claude\plans\federated-gathering-tome.md`.

> Owner: Riley (rileyhinsperger@gmail.com). Global CLAUDE.md forces **caveman mode** for chat replies (terse; code/commits normal). Multi-week project. Riley may start each session in a fresh thread — this file must carry all context so no rabbit hole is re-hit.
>
> **Git remote:** `origin` = https://github.com/rileyhins17/Atlas (**private**, GitHub account `rileyhins17`, gh CLI authed). **Push after every commit** (`git push`). `.env` is gitignored — never commit it.

## ⭐ COMMERCIAL-GRADE — permanent product bar (read every session)
Atlas is a product Riley intends to **sell** — build to a paid-SaaS standard, not a hobby project. Apply this without being asked.

**Definition of Done for every feature:**
- **Multi-tenant + authz:** every query scoped by `userId` (never trust client ids); one user can never read/write another's data.
- **Secure:** validate all input (zod at the boundary — already the pattern), rate-limit, security headers, CSRF protection for cookie-auth mutations, secrets encrypted (credentials already AES-GCM), no secrets in logs.
- **Robust:** typed errors, no unhandled rejections, pagination on list endpoints (no unbounded queries), sensible DB indexes.
- **Tested:** unit tests for logic (streaks, cost guard, crypto) + an e2e happy-path per module; CI runs build + typecheck + test.
- **Observable:** structured request logging with request ids; error tracking (Sentry-class) in prod.
- **Polished UX:** loading / error / empty states, responsive (mobile-first — it's a PWA), basic a11y, optimistic where it helps.
- **Legal/data:** sensitive data (journal, finance) → user-facing data export + hard delete; privacy policy + ToS before launch.
- **Billing (later):** Stripe subscription + plan gating.

**Already at bar:** unit tests (incl. cost-guard, tool loop, tool router, account export/delete) + **web component/hook tests** (`apps/web/test/*` on Vitest + React Testing Library + jsdom — primitives, Toast auto-dismiss, optimistic complete/check-in rollback, the global 400/401/500 error policy, client-side form validation) + GitHub Actions CI; tests covered by `pnpm typecheck` (per-package `tsconfig.test.json` — build config still excludes `test/` so tests never reach `dist`; web's Next build also excludes `test/` + `e2e/`); **Playwright + axe e2e** (`apps/web/e2e/*`) driving the real web+API+Postgres full-stack — happy-path register/task/habit flows + a dashboard a11y scan — run in a dedicated CI job against an ephemeral `pgvector/pgvector:pg16` service container (migrate deploy → build → `playwright install --with-deps chromium` → `test:e2e`); locally `pnpm --filter @atlas/web test:e2e` after `playwright install chromium`, against Neon; **user data export + hard account delete** (`modules/account`, GDPR-style right-to-access + right-to-erasure); rate limiting (`@nestjs/throttler`: 120/min global, 10/min login, 5/min register); security headers (helmet, JSON-only CSP); CSRF (sameSite=lax cookie + `OriginCheckMiddleware` on mutations); global error boundary (`AllExceptionsFilter` — no stack/internal leaks) + structured JSON request logging with `x-request-id` (`RequestIdMiddleware`); pagination on every list endpoint (shared `PaginationQuery`, hard cap 100); 1mb body limit; `trust proxy` for real client IPs behind Caddy.

**Known tracked debt (not yet at bar — see `docs/roadmap.md` Productization track):** no error tracking (Sentry-class) wired; no billing (Stripe); legal pages (privacy policy + ToS) not written — but **data export + hard delete are DONE** (see below); **e2e now exists** (Playwright happy-path + axe a11y, running in CI on a pgvector service container — see "Already at bar"); remaining e2e gap is breadth: only the core auth/task/habit routes are covered, not journal/notes/calendar/AI/settings, and the AI/Google flows aren't e2e'd (they need a stubbed provider). 2FA + password strength rules absent. `ai_usage.cachedPromptTokens` now records the prefix-cache hit count directly (a cache regression is silent — no error, just a ~3.5x bill — so this is the signal to watch); nothing alerts on it yet, and it's deliberately not surfaced in the UI. The 60s embedding sweep is in-process, so multiple API replicas would each run it (harmless — worst case duplicate work on the same rows — but it needs a lock or a real queue before scaling out). Don't let this list grow silently — either build to bar or add the gap here.

## What Atlas is
Personal "Life OS": one unified data layer for tasks, calendar, habits, journal, finance, plus a cheap cross-domain AI (DeepSeek, called direct) that briefs, auto-organizes messy input, nudges, chats over your life, and **asks you questions to fill its own gaps**. Self-hosted on a cheap VPS. Budget: AI credits < $5/mo. Accessible from phone + laptop (PWA).

Unique hook: silos become one graph; the AI curates its own knowledge by interviewing the user (`ai_questions` table surfaced as UI cards).

## Architecture spine (do not violate)
1. **Module = life-domain.** Each domain (tasks, calendar, …) is a self-contained NestJS module implementing `DomainModule` (`apps/api/src/core/domain-module.ts`): `aiContext(userId)` + `getToolSpecs()`, self-registers in `onModuleInit`. Adding a domain = copy `apps/api/src/modules/tasks/` shape. Core never changes.
2. **Connector = external API key.** Implements `Connector` (`packages/connectors/src/connector.ts`). Secrets stored AES-256-GCM encrypted in `credentials` table; a connector only gets a `ConnectorContext.getSecret()`. Registered in `ConnectorsService` (`apps/api/src/core/connectors.service.ts`).
3. **Unified timeline.** Every mutation also writes a `timeline_events` row via `TimelineService`. The AI reads this compact cross-domain log, never the whole DB.
4. **AI writes back.** `insights` + `ai_questions` are first-class tables so the AI accumulates knowledge cheaply. Cost is capped by `CostGuard` (`packages/ai/src/cost-guard.ts`) using `ai_usage` ledger + `AI_DAILY_TOKEN_CAP`.

## Stack + toolchain (verified on this machine)
- node v24, npm 11, **pnpm 11.13.1** (installed globally via npm), docker 29 + compose v5, git 2.53. Windows 11, PowerShell.
- TS monorepo: **pnpm workspaces + Turborepo**. **ESM everywhere.**
- API: **NestJS 11 (ESM), built with `tsc`** (see GOTCHA: not tsx). Prisma 6 + Postgres + pgvector. Web: **Next.js 15** PWA (React 19). Deploy: Docker Compose + Caddy; Cloudflare for DNS.
- **AI providers — chat is remote, embeddings are local:**
  - **Chat: DeepSeek direct API** (`api.deepseek.com`, model **`deepseek-v4-flash`**) via `DeepSeekConnector`. Chose direct over OpenRouter because Riley bought DeepSeek platform credits, not OpenRouter credits (see GOTCHAS).
  - **Embeddings: local, in-process** (`LocalEmbedder`, `packages/ai/src/local-embedder.ts`) using `@huggingface/transformers` + `Xenova/bge-base-en-v1.5` (768-dim, matches the `vector(768)` column). No API key, no per-call cost, no data leaves the box. DeepSeek has no embeddings API (verified: `POST /embeddings` → 404) and paying a second provider just for vectors wasn't worth it. **`OpenRouterConnector` has been deleted** — Atlas calls exactly one external AI provider.

## Repo map
```
packages/db        Prisma schema (FULL core model) + client singleton. Import DB ONLY via @atlas/db.
packages/shared    zod DTOs + enums + AI contracts (browser-safe, no DB import).
packages/connectors Connector interface + chat.ts (shared OpenAI-compatible types) + DeepSeekConnector (chat).
packages/ai        pricing (cache-aware), CostGuard, context-builder (token budgeting), tools.ts (wire-safe tool names),
                   orchestrator.ts (runToolLoop — provider-agnostic tool-calling loop), local-embedder.ts (in-process embeddings, no key).
apps/api           NestJS. core/ (prisma,crypto,timeline,domain-module,connectors,health,memory),
                   auth/ (scrypt+sessions+guard), modules/tasks|habits|journal|notes|calendar|finance (vertical slices),
                   modules/finance (FinanceService, FinanceAiAdapter, PlaidSyncService, plaid.controller — pull-only bank sync),
                   modules/ai (OrchestratorService, ToolRouterService, EmbeddingService, AiQuestionsService, ai.controller).
apps/web           Next 15. app/page.tsx = auth gate + tabs (Today/Habits/Calendar/Journal/Notes/Atlas AI). lib/api.ts = fetch wrapper.
infra/             (TODO) docker-compose, Caddyfile, Dockerfiles.
docs/              (TODO) architecture, data-model, roadmap, guides, ADRs, GOTCHAS.
```

## Current status (updated: 2026-07-16) — PHASE 0 COMPLETE ✅ (verified end-to-end)
- ✅ Monorepo + all 6 packages/apps build GREEN (`pnpm build`): db, shared, connectors, ai, api, web.
- ✅ Prisma schema migrated to a live DB. Migration `packages/db/prisma/migrations/*_init` created all 17 tables + `pgcrypto`/`vector` extensions + `embeddings.embedding vector(768)`.
- ✅ API verified over HTTP: `/health` ok, auth register/login/me/logout + 401 guard, tasks create/list/complete, `/ai/dry-run` (built context, logged 47 tokens to `ai_usage`, $0 spend), `/ai/status` (module registry reports domains `["tasks"]`).
- ✅ Persistence confirmed in DB: `timeline_events` got `task.created` + `task.completed` rows; `ai_usage` got the dry-run row. The unified-timeline backbone works.
- ✅ Web UI driven in a real browser: register → Today screen → add task → task persists and displays. Cross-port session cookie works.
- **LOCAL DB = Neon cloud Postgres** (Docker Desktop is broken on this machine — Model Runner crash, see GOTCHAS). `DATABASE_URL` in `.env` points at Neon. The VPS will use the Docker `db` service instead (Linux dockerd has no such bug). Neon has pgvector, so it's a faithful dev DB.
- ✅ Docs backbone written: `README.md`, `docs/architecture.md`, `docs/data-model.md`, `docs/roadmap.md`, `docs/module-guide.md`, `docs/connector-guide.md`, `docs/GOTCHAS.md`, `docs/adr/`.
- ✅ **Tests + CI (added 2026-07-16 overnight):** Vitest in `packages/ai` + `apps/api` (tests live in each package's `test/` dir, excluded from tsc build; `pnpm test` runs all via turbo). 27 unit tests: pricing, context-builder, password util, CryptoService (env vars stubbed in `apps/api/test/setup.ts` — no DB needed), habit streaks. `computeStreak`/`dayKey` extracted to `apps/api/src/modules/habits/habits.util.ts` (pure, exported; service imports it — behavior unchanged). GitHub Actions CI at `.github/workflows/ci.yml` (build + typecheck + test, no DB). `.gitattributes` normalizes line endings to LF. `esbuild` added to `allowBuilds` (vitest dep).

## How to run locally (verified working)
```
pnpm install
# .env already exists with Neon DATABASE_URL + generated secrets (gitignored).
pnpm build
# API (loads ../../.env via --env-file-if-exists):
pnpm --filter @atlas/api dev      # http://localhost:4000
# Web (separate shell):
pnpm --filter @atlas/web dev      # http://localhost:3000
```
Migrations: `cd packages/db` then set `$env:DATABASE_URL` from `.env` and run `pnpm exec prisma migrate dev`.

## Phase 1 progress
- ✅ **Habits** (`modules/habits`): CRUD + daily check-in, streak logic. Web `HabitsPanel`.
- ✅ **Journal** (`modules/journal`): mood + tags, tailored (NOT a generic journal) — writes timeline, **auto-queues to semantic memory** (`MemoryService.queueForEmbedding` → `embeddings` row, `model="pending"`, Phase 2 backfills the vector), and **seeds an `ai_question`** when an entry is thin/low-mood. Web `JournalPanel`.
- ✅ **Notes** (`modules/notes`): durable "what Atlas should know about me" facts; `pinned` = always-in-context; queued to memory. `summarize()` surfaces pinned facts. Web `NotesPanel`.
- ✅ **AI-questions loop** (the unique hook) live: `MemoryService.askUser()` creates them; endpoints `GET /ai/questions`, `POST /ai/questions/:id/answer|dismiss` (`modules/ai/ai-questions.service.ts`); answers get queued to memory as `qa`. Web: `AtlasAsks` cards at top of the dashboard.
- New core service: **`core/memory.service.ts`** (`MemoryService`, global) — `queueForEmbedding`, `removeFromEmbeddings`, `askUser`. Use it from any AI-native domain.
- Verified: low-mood journal → question created → answered; `embeddings` has pending rows for journal/note/qa; `ai/status` domains = `["tasks","habits","journal","notes"]`.

- ✅ **Calendar** (`modules/calendar`) DONE + verified: events CRUD over the `events` table, timeline `event.*`, pagination-bounded list (`MAX_PAGE`), zod validation (endAt≥startAt → 400), `summarize()` = next events. Web `CalendarPanel` (datetime-local inputs). `ai/status` domains = `["tasks","habits","journal","notes","calendar"]` (all 5). **Google OAuth sync intentionally NOT built yet** (biggest/riskiest piece — deferred to a fresh session so it isn't left half-done).

## Phase 2 — the AI brain (done: 2026-07-17, verified live against Neon + real DeepSeek key)
- ✅ **DeepSeek direct connector** (`packages/connectors/src/deepseek.ts`) — Riley bought DeepSeek platform credits (not OpenRouter), so chat goes straight to `api.deepseek.com` (model `deepseek-v4-flash`). `OpenRouterConnector` kept for embeddings only. Shared OpenAI-compatible chat types factored into `packages/connectors/src/chat.ts` so both connectors reuse one response parser.
- ✅ **Real pricing + cache-aware costing** (`packages/ai/src/pricing.ts`): `deepseek-v4-flash` ($0.14/$0.28 per 1M in/out, $0.0028 cached-in) and `deepseek-v4-pro` ($0.435/$0.87, $0.003625 cached-in), per the official pricing docs (verified 2026-07-17). Cache hits flow connector → `ChatUsage.cachedPromptTokens` → `CostGuard.record()`. Measured live: **~47 micro-USD/chat (~$0.00005)** with ~95% of prompt tokens cache-hit — the <$5/mo budget is not remotely at risk.
- ✅ **`runToolLoop`** (`packages/ai/src/orchestrator.ts`) — provider-agnostic, DB-free multi-turn tool-calling loop (send → if tool_calls, execute + feed results back → repeat up to `maxIterations`). Unit-tested in isolation with fake `chat`/`executeTool`.
- ✅ **Wire-safe tool names** (`packages/ai/src/tools.ts`, `toWireToolName`/`fromWireToolName`): Atlas's tool specs are dotted (`tasks.create`) but DeepSeek's function-calling API rejects any name outside `^[a-zA-Z0-9_-]+$` — dots become `__` on the wire and are converted back before hitting `ToolRouterService`. Discovered via a live 400 during verification; if you add a provider, check this first before assuming dotted names work.
- ✅ **`OrchestratorService`** (`apps/api/src/modules/ai/orchestrator.service.ts`) — every model call (including each tool-loop round-trip) is individually `costGuard.assertUnderCap()` → `deepseek.chat()` → `costGuard.record()`. Methods: `chat()`, `organizeBrainDump()`, `generateQuestions()` (AI-generated `ai_questions`, replacing the old journal heuristic — see `journal.service.ts`), `generateDailyBrief()` (writes `insights`, then best-effort calls `generateQuestions`), `listInsights()`.
- ✅ **`ToolRouterService`** (`apps/api/src/modules/ai/tool-router.service.ts`) — validates tool-call args with the same zod DTOs the HTTP boundary uses, routes to `tasks.create/complete`, `habits.log`, `journal.add`, `notes.remember`, `calendar.add`, `ai.ask_question` (→ `MemoryService.askUser`). Unit-tested with fake services (no NestJS DI needed).
- ✅ **Semantic memory — fully local, verified live.** `LocalEmbedder` lazily loads `Xenova/bge-base-en-v1.5` once per process (~110MB, cached on disk after the first download; ~7s cold load, ~110ms for 2 texts), emitting 768-dim mean-pooled + L2-normalized vectors, and throws `EmbeddingDimensionError` rather than write a wrong-width vector. `EmbeddingService` backfills `model="pending"` rows in chunks of 8 and runs `<->` similarity via `$queryRaw`. **No CostGuard on this path — local inference is free**, so there is nothing to cap (that's the point of choosing local). Verified: backfilled 2 rows (0 failed, idempotent on re-run), Neon confirms `vector_dims=768` / `norm=1`, and ranking is sane — "I am stressed about my job" → anxiety journal entry at 0.64 vs unrelated note at 0.98, with zero keyword overlap.
- ✅ **Retrieval wired into chat** (`OrchestratorService.recallText`): embeds the user's message, takes the top 3 matches under a 0.9 distance threshold, appends them **to the final user message** (never the system prompt — see the prompt-order gotcha; that placement is worth ~3.5x on cost). Best-effort — recall failure logs a warning and chat continues. This does what module summaries structurally can't: surface an old entry that's topically relevant but isn't "current state". Verified live: "Have I mentioned feeling any pressure at work recently?" quoted the journal entry back.
- ✅ **Embedding backfill is automatic** — `EmbeddingService.sweepPending()` runs on a 60s `@Interval` (`ScheduleModule.forRoot()` in `app.module.ts`) across all users, with a re-entrancy guard (embedding is CPU-bound + single-threaded here). Writes stay fast — `MemoryService` only queues; nothing embeds inline. The model is warmed at boot (`onApplicationBootstrap`, non-blocking, ~700ms warm / ~7s cold) so the first user doesn't pay the load. Verified live: a new journal entry plus 3 rows stranded from earlier sessions were swept and became recallable within a minute.
- ✅ Endpoints on `AiController`: `POST /ai/connect/deepseek`, `POST /ai/chat`, `POST /ai/brain-dump`, `POST /ai/daily-brief`, `GET /ai/insights`, `POST /ai/questions/generate`, `POST /ai/embeddings/backfill` (plus existing status/dry-run/questions CRUD).
- ✅ Web: new "Atlas AI" tab (`apps/web/app/page.tsx`) — connect-key form (shown until configured), `ChatPanel` (multi-turn, shows which tools ran), `BrainDumpPanel`, `DailyBriefPanel` (generate + history list).
- ✅ **Tests:** `packages/ai/test/cost-guard.test.ts` (mocks `@atlas/db`'s prisma singleton via `vi.mock`, closing the "no cost-guard tests" debt item), `orchestrator.test.ts` (tool loop: no-tool passthrough, single/multi tool call, failed tool execution fed back to the model, malformed JSON args, iteration cap), `tools.test.ts` (wire-name mapping), `pricing.test.ts` (v4 rates, cache-hit split, clamping). `packages/connectors/test/chat.test.ts` (usage/cache-token parsing, both DeepSeek + OpenAI shapes). `apps/api/test/tool-router.test.ts` (routing + zod validation per tool, unknown-tool rejection). 67 tests total, all green; `pnpm build`/`typecheck`/`test` all pass.
- ✅ **Verified live** against the Neon DB with Riley's real DeepSeek key: registered a test user, connected the key, ran `/ai/chat` (created a real task via tool call), multi-turn history recall, `/ai/brain-dump` (filed one input into a task + journal entry + pinned note across 3 domains in one call), `/ai/daily-brief` (wrote an `insights` row, then auto-generated 2 relevant `ai_questions` — no heuristic involved), confirmed `ai_usage` accumulated correctly (6997/200000 tokens) and stayed under `AI_DAILY_TOKEN_CAP`.

## Google Calendar (built 2026-07-17) — ✅ VERIFIED LIVE 2026-07-19
Riley completed the OAuth consent flow. Confirmed in Neon: tokens stored **AES-GCM encrypted** in `credentials` (672-char ciphertext, not plaintext; meta has scope + connectedAt + lastSyncedAt), and **92 Google events synced** into `events` (`source='google-calendar'`) alongside 10 `source='atlas'` events. The `configured/connected` status endpoint and the Settings connect→consent→callback→sync path all work end-to-end. **Still untested:** the token-refresh path (force it by backdating a stored `expiresAt`) and the delete-propagation/push-flip edges — cheap to check next time. The `.env` already holds a real Google OAuth client (`GOOGLE_CLIENT_ID`/`SECRET`/`REDIRECT_URI`); consent-screen test-user is configured in Google Cloud.
- `packages/connectors/src/google-calendar.ts` — `GoogleCalendarConnector`: `authUrl()`, `exchangeCode()`, auto token refresh (persists via the new `ConnectorContext.saveSecret()`), `listEvents()` (paginated, `singleEvents=true` so recurrences arrive as concrete instances, `showDeleted=true` so deletions propagate), `createEvent()`, `updateEvent()`. Scope: `calendar.events` only.
- `apps/api/src/modules/calendar/google-sync.service.ts` — the reconciliation. Connectors have **no DB access by design**, so `Connector.sync()` was removed from the interface (it could never have worked) and the module owns this instead.
- **Sync policy (Riley chose): two-way, Google wins.** Pull overwrites the Atlas row; Atlas-authored events (`source='atlas'`) push once then flip to `source='google-calendar'` + `externalId` so later pulls match instead of duplicating. **Atlas never deletes from Google** — a local delete stays local; only a Google-side deletion removes the Atlas row. Window: −30d/+365d, ≤50 pushes/run. See `docs/adr/0004-google-calendar-sync.md`.
- `oauth-state.ts` — HMAC-signed, user-bound, 10-min TTL. CSRF control for the handshake **both ways**: stops a forged start, and stops a victim being walked through a callback that attaches an *attacker's* Google account to their Atlas account (which would mirror their calendar out). Unit-tested incl. tamper/expiry/length-mismatch.
- Env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` (all optional — unset ⇒ connector unregistered and Settings shows "unavailable"; Atlas runs fine without it).
- Web: **Settings tab** — connect / sync now / disconnect, reads `?google=connected|denied` from the callback redirect.
- **Fixed a real multi-tenant bug found here:** `Event`/`Account`/`Transaction` had `@@unique([source, externalId])` — **global, not per-user**. A calendar event shared between two Atlas users carries the same Google id for both, so the second user's upsert would collide with and overwrite the first's row. Now `@@unique([userId, source, externalId])` (migration `20260717132500_scope_external_ids_per_user`).

## Account data rights (built 2026-07-17, verified live + in-browser)
Commercial-grade privacy bar: `apps/api/src/modules/account/`.
- **Export** — `GET /account/export` streams a downloadable JSON file (`atlas-export-<date>.json`) of everything the user owns (tasks, events, habits, logs, journal, notes, goals, accounts, transactions, timeline, insights, ai_questions, embedded *content*). **Secrets are excluded by construction** — no `passwordHash`, no session tokens, no `credential.dataEnc` (connections are reported as metadata only). BigInt minor-unit amounts are serialized via a replacer. Verified live: created cross-domain data + a stored DeepSeek key, exported, confirmed the API key / passwordHash / dataEnc strings are all absent from the file.
- **Hard delete** — `POST /account/delete` requires `{ password, confirm: "DELETE" }` (re-auth + intent guard; session alone is not enough for an irreversible destructive action), throttled 5/min. `user.delete()` cascades every domain table (schema `onDelete: Cascade`); `ai_usage` rows survive de-identified (`onDelete: SetNull`) so the cost ledger keeps working without PII. Clears the session cookie. Verified live: wrong password → 401, bad confirm → 400, correct → 201 + cascade confirmed in Neon (user + all child rows gone). Also driven in-browser: Settings → Delete → password gate → bounces to auth gate.
- Web: **Settings → Data & privacy** — Export button (blob download) + Delete-account flow (reveals a password field, not a one-click action).

## Life-OS UI v2 (built 2026-07-19 overnight session, per `docs/atlas-ui-vision.md`) ✅
The ground-up front-end redesign — the "7 CRUD pages" pattern is dead. All on branch `claude/session-check-in-mbo5om` (PR #1). **Verified live in-browser + 10/10 Playwright e2e green (incl. two axe scans).**
- **Ambient AI, not a tab.** `/ai` is deleted (redirects `/today`). `components/atlas/`: `AtlasUiProvider` (one global hotkey listener: **⌘K** command bar, **⌘J** chat rail, **⌘\ ** sidebar; chat transcript survives rail close; sidebar pref in localStorage), `CommandBar` (Radix; type anything → **capture** via `/ai/brain-dump` with a "Filed: 1 task, 1 journal entry" toast (`summarizeToolRuns`), `?…` → **ask** (auto-sends in the chat rail), plain words → fuzzy **jump**; full combobox/listbox ARIA + arrow-key nav), `ChatRail` (right slide-over chat-over-your-life). DeepSeek key + token meter moved to Settings (`AiSettingsCard`).
- **Home = command center** (`components/home/`): date + time-aware greeting, `HeroBrief` (latest insight, generate/refresh, warm not-connected state), ⌘K capture pill, AtlasAsks inline, then a 2-col grid (stacks ≤900px): `DaySpine` (today's events + time-boxed tasks, chronological with a live "now" divider, past items recede), `FocusTasks` (Overdue/Today/Next, capped, due-then-priority), `HabitRings` (tappable `ProgressRing`s, optimistic check-in, streak flames), `PulseCard` (mood word + 14-entry sparkline + week count).
- **Timeline/Story view** (`/timeline`): the `timeline_events` stream made visible — day-grouped spine, per-type Lucide glyphs, domain filter chips, infinite "Show more". Backend: new read-only `modules/timeline` (`GET /timeline`, offset-paginated with a `hasMore` over-fetch, optional `source` filter) + `GET /habits/history?days=7..366` (day-keyed counts per habit) — both zod-validated, userId-scoped, unit-tested.
- **Rich domain views:** Tasks (`/tasks`) grouped Overdue/Today/This week/Later/No date + collapsed Done, shared `TaskRow` (inline title edit, priority-flag cycling via optimistic `useUpdateTask` PATCH, warm due chips from `lib/dates.ts` `formatDue`); Habits = per-habit cards (check-in, 7-day dot grid, 12-week `Heatmap`, streak); Journal + mood-over-time `Sparkline`; Calendar = forward agenda grouped by day; Notes = knowledge-card grid with pinned facts elevated.
- **Shell v2:** collapsible sidebar (icon rail, persisted), "Ask or add…" pill, nav = Home/Tasks/Calendar/Habits/Journal/Notes/Timeline/Settings (mobile bottom bar keeps 5; Settings icon in the mobile top bar). Wide pages opt in via `.page-wide` (`.main-inner:has(> .page-wide)` → 1240px).
- **New primitives** (`components/ui/`): `ProgressRing`, `Sparkline`, `Heatmap` (target-relative intensity, future-day dimming), `Kbd`; `lib/dates.ts` (formatDue/formatAgo/greeting/day math — pure, tested).
- **Tests:** +38 unit (dates, ring/sparkline/heatmap, tool-run summariser, groupTasks/weekCells/groupEventsByDay, timeline+history services) and a new e2e file `apps/web/e2e/life-os.spec.ts` (⌘K jump+Esc, ⌘J focus, sidebar persistence, Home zones + ring check-in, cross-domain timeline + filter, axe on Home with the command bar open).

## Phase 3 — Finance domain + Plaid connector (built + ✅ SANDBOX-VERIFIED LIVE 2026-07-19)
Money is now a life-domain the AI reads. Provider-agnostic `finance` domain + a `Plaid` connector behind it (the domain knows nothing about Plaid). Built to bar: userId-scoped, zod-validated, paginated, encrypted item tokens, unit-tested. Full build + typecheck + all tests green (37 connectors incl. 13 Plaid, 54 api incl. plaid-sync + finance-dto, 41 web). See `docs/adr/0005-plaid-finance-connector.md`.
- **Why Plaid:** Canada has no consumer bank APIs (open banking not live). Plaid is the only self-serve, free-to-dev aggregator with Canadian coverage (Flinks = enterprise-only; Salt Edge = backup).
- **`packages/connectors/src/plaid.ts`** (`PlaidConnector`, raw fetch — no `plaid` SDK, same style as google-calendar.ts): `createLinkToken` (update mode when given an access token), `exchangePublicToken`, `getAccounts`, `syncTransactions` (cursor-paged), `removeItem`, `sandboxCreatePublicToken` (drives the whole path with no human), `verify`. Pure exported mappers `plaidAmountToMinor`/`mapPlaidAccountType`/`plaidCurrency`.
- **`modules/finance/`**: `finance.service.ts` (accounts+transactions CRUD, `summarize()` = balances + 7-day cash-flow), `finance.ai.ts` (`DomainModule`, **no tool specs** — AI reads money, doesn't move it), `finance.controller.ts` (`/finance/accounts`, `/finance/transactions`), `plaid-sync.service.ts` (pull-only reconciliation, one bank = one credential keyed `label=itemId`, cursor in `meta`), `plaid.controller.ts` (`/connectors/plaid/status|link-token|exchange|sync|disconnect`, + a deferred no-op `webhook`). Registered in `app.module.ts` → domains now `["tasks","habits","journal","notes","calendar","finance"]`.
- **Web:** Settings **Plaid card** (`components/panels/PlaidCard.tsx`, `react-plaid-link` `usePlaidLink`) + new **`/finance` page** (accounts w/ balances + day-grouped transactions) + `Finance` in the sidebar nav.
- **DB:** additive nullable migration `20260719160000_finance_plaid_fields` (`accounts.mask/institution`, `transactions.pending/merchantName`). Applied via `migrate deploy` — see the Neon-drift GOTCHA.
- **✅ Verified live in sandbox (2026-07-19):** sandbox keys are in `.env` (`PLAID_ENV=sandbox`). Drove `sandboxCreatePublicToken` → `/exchange` → `/sync` for a real user against `api.sandbox.plaid.com`: item token stored **AES-GCM encrypted** (no plaintext `access_token` in `dataEnc`), meta holds `cursor`/`institution`/`connectedAt`/`lastSyncedAt`, **12 accounts + 48 transactions** imported (`source='plaid'`, First Platypus Bank), types/masks/balances correct, **sign correct both ways** (Uber −540 out; INTRST PYMNT +422 in), and re-sync is **idempotent** (imported 0 once the sandbox cursor drained — Plaid delivers sandbox txns in waves across the first few syncs, expected). **Not yet done:** in-browser Plaid Link flow (works from Settings with sandbox creds `user_good`/`pass_good`), and **production** (real Canadian bank = `PLAID_ENV=production` + prod secret + Plaid production approval + registered `PLAID_REDIRECT_URI`; no code change). Prod secret was pasted in a chat session — rotate before launch.

## Phase 4 — Proactive engine (STARTED 2026-07-19) — first slice in
Making Atlas act on its own instead of waiting for button presses.
- ✅ **`modules/ai/proactive.service.ts` (`ProactiveService`)** — an `@Interval` hourly sweep (mirrors `EmbeddingService.sweepPending`: re-entrancy guard, scans users) that generates, for each user who has AI configured (a `deepseek` credential): a **daily brief** once per UTC day, and a **weekly review** once per UTC week (Monday-anchored). Both reuse `OrchestratorService` (so **CostGuard-capped**) and seed fresh `ai_questions` — nudges come free. Generic `generateMissing(users, kind, since, generate)` helper drives both off one `configuredUserIds()` query; bounded 50 users/sweep; per-user failures caught + retried next sweep. Registered in `AiModule`.
- ✅ **`OrchestratorService.generateWeeklyReview(userId)`** — cross-domain 7-day reflective review (wider activity window + pattern-finding prompt), writes `insight` `kind='weekly_review'`. Endpoint `POST /ai/weekly-review` (+ `AiApi.weeklyReview` web client) for manual trigger; auto-fires via the sweep. Both daily & weekly land in the existing `/ai/insights` history + Home `HeroBrief`.
- ✅ **Per-user schedule + timezone prefs.** The sweep now honours each user: only opted-in users (`User.proactiveEnabled`) with a DeepSeek credential, and only once their local `User.briefHour` (in `User.timezone`) has arrived — daily brief once per **local** day, weekly review once per **local** week (Monday). Timezone math is `Intl`-only (no tz-data dep) in `modules/ai/time.util.ts` (`tzOffsetMs`/`localHour`/`localDayStartUtc`/`localWeekStartUtc`, unit-tested with fixed dates incl. an invalid-tz→UTC fallback; ±1h on the 2 DST-transition days is accepted). New `modules/settings` (`GET`/`PATCH /settings`, zod + tz validation → 400 on a bad zone) + web **Proactive AI card** in Settings (enable toggle, timezone w/ Detect, brief hour) with `SettingsApi`/`useSettings`. Migration `20260719163000_user_proactive_prefs` (`User.proactiveEnabled` default true, `User.briefHour` default 7). Verified live: defaults `UTC`/`7`/`true`, PATCH persists, bad tz → 400.
- Unit-tested (`apps/api/test/proactive.test.ts` both kinds + eligibility + briefHour gate + failure-resilience; `time-util.test.ts`). Build + typecheck + **64 api tests** green.
- ✅ **Nudge delivery via Web Push.** When the sweep generates a brief/review it fires a best-effort web-push notification. Self-issued **VAPID** keypair (`VAPID_PUBLIC_KEY`/`PRIVATE_KEY`/`SUBJECT` in `.env`, generated via `web-push`, no external account — optional: unset ⇒ push unconfigured, UI shows unavailable). New `push_subscriptions` table (migration `20260719164500_push_subscriptions`, one row/device, endpoint unique). `modules/push`: `PushService.sendToUser` (loops a user's subs, `web-push` `sendNotification`, **prunes 404/410** stale subs) + `GET /push/public-key`, `POST /push/subscribe|unsubscribe`. SW gained `push` + `notificationclick` handlers (`public/sw.js`). Web: `lib/push.ts` (permission → `PushManager.subscribe` with the VAPID key → POST) + Enable/Disable in the Proactive AI settings card. Unit-tested (`push.test.ts`: send-to-all + 410 prune, `web-push` mocked). **Verified server-side live** (public-key configured:true, subscribe stores, bad sub → 400). ⚠️ **Browser subscribe + actual delivery NOT verifiable here** — the SW registers **prod-only** (`ServiceWorkerRegistrar`) and push needs a real browser + the vendor push service. `lib/push.ts` self-registers the SW on enable so it can work in dev too, but delivery still needs a real browser. Verify by: open the deployed/prod app → Settings → Enable notifications → generate a brief → confirm the notification.
- **Known limits / next:** proactive AI spend should become **opt-in / plan-gated** at the plan level before multi-tenant launch (per-user on/off exists; billing gating doesn't). Next Phase-4 pieces: a dedicated weekly-review UI card, configurable weekly-review day, and push for new `ai_questions` (currently only briefs/reviews push).

## Brand + Home refresh (2026-07-19)
- **New logo — "Constellation".** Replaced the off-brand blue crosshair with a coral (`#f2a98f→#b5502f`) constellation "A" (life domains as one connected graph). Shipped in `public/icon.svg`, `public/icon-maskable.svg` (glyph in safe zone), and `components/Logo.tsx` (sidebar/gate/mobile). Reusable motif component `components/atlas/Constellation.tsx` (`animated` draw-in + `lonely` empty-state variant).
- **Home is capture-first** (`components/home/HomeDashboard.tsx`). Top of Home is now a real always-there **capture box** (`HomeCapture.tsx`) — type plain words → `useBrainDump` files it via the AI → "Filed: …" toast (reuses `summarizeToolRuns`). Replaces the ⌘K-only button; anyone can use it with no shortcuts. **First-run onboarding** (`HomeOnboarding.tsx`): a user with no tasks/events/habits gets an animated constellation + explanation + the capture box + example chips instead of empty cards. CSS in `globals.css` (`.home-capture*`, `.home-onboarding`, `.constellation.animated` keyframes; reduced-motion respected).
- ✅ **Motif everywhere.** `EmptyState` (ui primitive) now renders the lonely constellation by default (one bright node among faint ones) instead of per-domain lucide icons — so all 8 domains' empties + Home zones read as one surface, zero call-site churn (the `icon` prop is kept but ignored). `Constellation` gained a `loading` variant (looping node pulse, reduced-motion-safe); wired into `HeroBrief`'s loading state ("Reading your day…"). Reusable for any future "Atlas is thinking" moment.
- **Timeline-as-primary-surface** idea captured in `docs/roadmap.md` backlog (Riley wants `/timeline` to become the main interactive page — big rethink, later).

## UX v3 — "The Stream" (2026-07-19) ✅ — Timeline IS the app
Riley's directive: "the timeline should be the main page you interact with." Home and the Timeline page merged into **one interactive feed** — plan at `.claude/plans/scalable-weaving-leaf.md` (UX v3 revision).
- **IA:** `/today` renders `components/stream/StreamPage.tsx`; `/timeline` **redirects** to `/today`; NavBar's Timeline entry removed. Retired + deleted: `HomeDashboard`, `DaySpine`, `FocusTasks`, `PulseCard`, `TimelinePanel` (superseded; domain pages untouched).
- **Layout (down = older, social-feed familiar):** sticky capture (`.stream-capture`, blur backdrop) → NowCluster (greeting + `HeroBrief` + `HabitRings` + `AtlasAsks`) → **UpNext** (bounded plan: overdue ⚠ → rest of today → "+N tomorrow" chip; one-tap task complete) → **NowLine** (live minute clock, pulsing brand dot) → **Feed** (`timeline_events` newest-first, day-grouped, domain filter chips incl. Finance, infinite "Show earlier").
- **Interactivity:** capture files via brain-dump then invalidates `['timeline']` so rows appear instantly; leading `?` opens the chat rail pre-asked (both capture + ⌘K share the convention); feed rows deep-link by `refType`; a `task.created` row whose task is still open renders a **live complete-checkbox** in the feed (`openTaskRef` cross-refs the cached `useTasks()` set — zero API changes in the whole build).
- **Pure logic in `apps/web/lib/stream.ts`** (`buildUpNext` / `groupFeedByDay` / `openTaskRef` / `feedRowHref`) — fixed-date unit tests in `test/stream.test.ts`. First-run users get `HomeOnboarding` inside the stream shell (e2e note: a fresh account shows onboarding, so stream specs seed data first).
- **Found + fixed while shipping:** (1) `grouping.test.ts` was time-flaky after 9pm (`now+3h` crossed midnight) — `groupEventsByDay` now takes an optional `now`, test uses a fixed noon; (2) `.chip.active` failed WCAG AA at 3.93:1 once chips landed on an axe-scanned page — now solid `--accent-strong` + white (~5.5:1); (3) `screenshots.spec.ts` is gated behind `SHOTS=1` (its extra registration tripped the 5/min register throttle in full runs) and its theme step is theme-agnostic.
- **Green:** build 6/6, typecheck 10/10, unit tests all pass (49 web incl. 7 stream), **e2e 10/10 + axe** (rig skipped by design).

## UX v3.1 — feed-first Stream + onboarding loop fix (2026-07-20) ✅
Riley used the live app and flagged two things: "the timeline literally is not the app" (the feed was buried ~575px down behind a stacked dashboard) and the onboarding wizard "doesn't seem to be really working that well". Both fixed + **verified live in a real 1440×900 Chromium** (the browser-pane is 0×0 here — the GOTCHA — so I measured with a throwaway Playwright script instead).
- **The now-material is now ONE compact strip, not a stacked dashboard.** New `components/stream/NowStrip.tsx` folds greeting + brief + habits + plan into a single ~134px strip (was greeting 89 + hero card 91 + rings 125 + up-next 26 ≈ 330px+ of stack). Retired + deleted: `NowCluster`, `UpNext` (the component; `buildUpNext` logic stays in `lib/stream.ts`), and `home/HabitRings`.
  - Habit rings → `components/stream/HabitChips.tsx`: small inline pill chips (20px ring + name + streak, one-tap check-in), not a 125px card.
  - Daily brief → `HeroBrief` gained a `compact` prop: a single clamped line with an inline spark, expandable in place (`.clamped-1`).
  - Up Next → a one-line `GlanceLine` inside NowStrip: "N overdue · Next {thing} {time} · +N tomorrow" (pure `upNextGlance()` in `lib/stream.ts`, unit-tested). Falls back to the next routine block when the plan is clear.
- **Measured result (1440×900, seeded account):** now-line top **276px**, feed top **313px**, first feed row **400px** — the timeline is above-the-fold and primary. `.nowstrip` = 134px. (Was: feed content started ~575px down.)
- **Onboarding first-run loop FIXED.** `StreamPage`'s first-run gate now also checks the routine (`useRoutine`). The wizard's `finish()` ALWAYS writes routine blocks (Sleep + Wind-down are unconditional in `buildRoutine`), but a user who finished without picking habits or creating tasks kept `isFirstRun` true → the wizard remounted / got stuck on the "Mapping your week…" screen forever. Gating on routine flips first-run off regardless of habit picks. **Verified live:** a fresh account with only routine blocks now lands on the stream, not the wizard.
- **Onboarding a11y bug fixed** (pre-existing, caught by the e2e axe scan): `<div class="onb-dots" aria-label>` → added `role="group"` (`aria-label` is prohibited on a role-less div — `aria-prohibited-attr`, serious).
- **e2e hardening:** `happy-path.spec.ts` registered a fresh user in every test (4) which, combined with `life-os`'s 1, tripped the 5/min register throttle in a full run (the documented anti-pattern). Refactored to one shared account (storage state) for the CRUD/axe tests + one throwaway for the sign-out/sign-in cycle (session-destroying, can't share) → 2 registers instead of 4. The axe test now scans the real populated stream instead of the wizard (waits for a `.feed-row` so the scan is deterministic — it was a false pass locally when the feed hadn't rendered yet).
- **Fixed a real WCAG AA contrast bug the new axe scope surfaced** (CI-only at first — the browser-pane can't run axe here): `.feed-time` used `color-mix(--muted 65% transparent)`, which composited to **#a09990 on the light #faf7f2 surface = 2.63:1** (< 4.5). Now solid `var(--text-muted)` (#6f665c light / #a89f93 dark — both clear AA). Verified 0 contrast violations in a forced-light-theme axe pass on a populated feed.
- **Fixed a shell-level mobile layout bug** (found while screenshotting the app on a 390px viewport): `.app-layout` is a flex ROW, and below 900px the sidebar hides but `.mobile-topbar` becomes `display:flex` — so the top bar became a row sibling of `.main`, eating ~300px and squeezing the whole page into an ~90px sliver (content wrapped one word per line). Fix: `.app-layout { flex-direction: column }` inside the `max-width:900px` query so the top bar stacks full-width above a full-width canvas. Affected every page on mobile, not just the stream. Verified at 390px: no horizontal overflow, stream full-width. (No e2e covers a mobile viewport — worth adding.)
- **Stabilized a pre-existing ⌘J chat-rail focus flake** (headless CI: the input mounted but never took focus): `ChatRail` now also focuses on the next animation frame (`requestAnimationFrame`) so the entrance animation can't drop the focus.
- **Green:** build 6/6, typecheck 10/10, unit 61 web (all pass, +2 `upNextGlance`), **full e2e 10/10 + axe green (verified twice)** (screenshots rig skipped by design). NOTE: the CI `e2e` job (pgvector service container, LIGHT theme) is stricter than a local dark-theme run — axe contrast regressions can pass locally and fail CI. When touching muted text or the feed, verify with a forced `colorScheme:'light'` + `data-theme='light'` axe pass (throwaway Playwright script — the browser-pane is 0×0 and can't do it).

## UX v4 — The Day Canvas (2026-07-21) ✅ Phase A shipped
Approved ground-up rethink (design doc: **`docs/atlas-design-v4.md`** — read it before touching the home surface; plan `scalable-weaving-leaf.md`). Home stopped being a feed: **/today renders the day as a vertical sequence of time sections** — routine blocks as kind-tinted background washes ("every hour of every day filled"), calendar events + due tasks (live checkbox) + actuals (timeline rows, CRUD noise filtered by `CANVAS_NOISE_TYPES`) as cards inside them, a pulsing now-line inside the current section ("Now · 12:01 PM — Work"), auto-scroll-to-now on load. Open gaps are tappable → capture focuses with the time window attached (the window rides into brain-dump as a parenthesised hint so the AI files into that slot). Day pager (‹ Yesterday · Today · Tomorrow ›; title snaps back to today) — continuous bidirectional scroll deliberately deferred (Phase D).
- **Engine:** `apps/web/lib/canvas.ts` `buildDayCanvas()` — pure, 11 fixed-date unit tests (`test/canvas.test.ts`): overnight-wrap split (yesterday's mask governs the morning tail), overlap clamping, sub-5-min gap suppression, item placement, now-index, flavors. Components in `components/canvas/` (TodayView/DayCanvas/DayPager/TimeSection/CanvasCard). API additions (backward-compatible): `TimelineQuery` + `from`/`to` (≤62d window), `EventListQuery` on GET /events.
- **IA:** nav = **Today · History · Progress** + collapsible persisted **Manage** group (6 domain pages) + Settings. `/history` = the v3 feed (Feed/FeedRow intact); `/progress` = placeholder until Phase C; `/timeline` still redirects. Mobile bar: Today/History/Progress/Tasks/Settings. `StreamPage` deleted; `NowStrip` gained `showGlance` (canvas shows the plan itself).
- **Gotcha found live:** a mount-time `animation-fill-mode: both` slide froze on its 0% frame in a throttled/background tab, shifting the canvas 28px and overflowing mobile — page-transition animations now apply only on actual paging, never first mount.
- Green: build, typecheck, 220 unit tests, e2e 10/10 (canvas + history specs + two axe scans). Verified live: test account's routine renders the full day (Sleep tail → Open → Work w/ now-line → Dinner → Sleep head), mobile 375px overflow-free.
- ✅ **Phase B shipped (2026-07-21): onboarding v2 + loading screens.** The MCQ wizard is gone: a warm, conversational **form** — welcome+name → real `<input type="time">` sleep times → weekday shape select with exact work hours revealed for fixed schedules + exercise/meals selects → **three free-text steps ("About me" / "My goals" / "Things to know") that become PINNED notes** (tags ['onboarding'], only non-empty) — always in the AI context via the notes summary and auto-embedded by the memory pipeline → habits (seed chips + free-text add) → an `AtlasLoadingScreen` build step (staged messages). `AtlasLoadingScreen` (components/atlas/) also replaced the bare "Loading…" auth gate. `lib/onboarding.ts` v2: exact-minute `buildRoutine` (workStart/EndMin), `timeToMin`/`minToTime`, `answersToNotes` — all unit-tested. New e2e `a-onboarding.spec.ts` walks the whole form into a routine-backed canvas + pinned notes (named a-… to register first — throttle budget). `register()` helper retries once on the cold-start first-request flake (fresh Neon + unwarmed API).
- **Dead code pruned with Phase B** (cleanliness bar: no dead exports at commit): `lib/stream.ts` now holds only live logic (groupFeedByDay/openTaskRef/routineAt/feedRowHref) — buildUpNext/upNextGlance/nextRoutine/formatMinute + NowStrip's GlanceLine deleted with their tests (superseded by the canvas).
- **Next:** Phase C (/progress stats), Phase D (continuous scroll, drag-reschedule).

## UX v4 Phase C — Progress (statistics) (2026-07-22) ✅
The "synthesized life statistics" backlog item. New read-only **`modules/stats`**: `GET /stats?days=7..365` returns `{days: [{day, tasksCompleted, habitChecks, moodAvg, spentMinor, earnedMinor, events}], totals: {current, previous}}` — five raw-SQL aggregates bucketed by **the user's local day** (`AT TIME ZONE ${tz}`, tz bound as a parameter, never interpolated), each query spanning the requested window AND the one before it so the UI can show deltas without a second round-trip. Zero-filling + window splitting live in the pure `assembleStats()` (`stats.assemble.ts`, unit-tested without a DB).
- **`/progress`**: range chips (30/90/365) → headline tiles (big number + ↑/↓ delta chip vs the previous window; spend inverts so "down" reads as good) → activity `Heatmap` → weekly trend `Sparkline`s. Pure helpers in `lib/progress.ts` (`delta`, `weeklyBuckets`, `moodSeries` — mood carries the last value across journal-less days so the line is readable).
- **AI payoff:** `StatsService.summarizeForAi()` appends a 30-day-vs-previous-30 summary to the **weekly review** prompt (appended LAST — the prefix-cache order gotcha), so the review can spot real cross-domain patterns. Zero new model calls.
- **Two bugs caught by live verification that unit tests structurally could not** (raw SQL needs a real DB): `habit_logs` has **`loggedAt`, not `createdAt`** (500 on every call), and journal mood must key off **`entryDate`** (the day the entry is *about*) rather than `createdAt`. Verified live end-to-end afterwards, including tz correctness: a journal entry at `02:00Z` correctly buckets to the **previous** local day for an `America/Toronto` user.
- **Cleanliness sweep** (standing rule — the codebase must be clean before every commit): consolidated three sets of duplicated helpers — `safeTz`/`dayKeyInTz` now exported once from `modules/ai/time.util.ts`; `dayBit` + `localMidnight` folded into `lib/dates.ts` (`localMidnight` was a third copy of the existing `startOfDay`); money formatting unified in **`lib/money.ts`** (`formatMoney` for the signed ledger, `formatMinorCompact` for stat tiles). Deleted 24 dead CSS rules left by the retired `UpNext`/`GlanceLine` components.
- **New a11y token:** `--success-text` (`#4ade80` dark / `#0f6b32` light). `--success-role` is tuned for fills/rings where 3:1 suffices; small bold labels need 4.5:1 and now use the text-grade token — the delta chip failed axe at 2.98:1 before this.
- Green: build, typecheck, **229 unit tests**, **e2e 12/12** (incl. three axe scans).

## v6 — recurrence, NL scheduling, AI guardrails, Progress polish (2026-07-24) ✅
Riley's v6 feedback batch. Plan: `.claude/plans/scalable-weaving-leaf.md`. **Apple Reminders was investigated and skipped** — Apple ships no REST API for Reminders; CalDAV `VTODO` is the only route, needs a hand-made app-specific password, is undocumented, and he's on Windows. Revisit only if Reminders becomes where he actually lives.
- **Recurrence (RRULE).** New `packages/shared/src/dto/recurrence.ts`: `parseRrule`/`formatRrule`/`describeRrule`/`nextOccurrences`/`nextOccurrence` + `RECURRENCE_PRESETS`. Supported subset = `FREQ=DAILY|WEEKLY|MONTHLY` + `INTERVAL`/`BYDAY`/`COUNT`/`UNTIL`; **anything else is stored verbatim and never expanded**, so a Google round-trip can't destroy it. `@atlas/shared` gained its own vitest rig (19 tests). Schema: nullable `recurrence` + `recurrenceParentId` on `Task` and `Event` (migration `20260724120000_recurrence_fields`, applied with the Neon-safe `migrate diff` → hand-written SQL → `migrate deploy` workflow).
  - **Tasks materialise lazily** — one open instance per series; `TasksService.spawnNextInstance` creates the next on completion (from BOTH `POST /complete` and `PATCH status=DONE`), anchored on the **series root** so monthly-on-the-31st can't drift after February clamps it. No cron, nothing accumulates.
  - **Events expand on READ** inside `CalendarService.list(from,to)`. Synthetic rows carry `id="<rootId>@<epochMs>"` + `isOccurrence:true` and are not editable (EXDATE per-instance exceptions are out of scope). `useEvents()` had to gain a 60-day window — **without one the server can't expand and a weekly event shows exactly once**.
  - **UI:** `RecurrencePicker` (preset chips — repeat is a tap, never a typed RRULE) in the task quick-add and the calendar form; `describeRrule` renders "Every weekday" on `TaskRow` and calendar rows.
- **Natural-language scheduling.** Root cause found: the AI was **never told the current date, time or timezone**, so "tomorrow at 2 PM" was the model guessing today's date from training data. `OrchestratorService.nowBlock()` now prepends a `## Now` block (user's local date/time/weekday + IANA zone) to the snapshot, chat, and `organizeBrainDump` (which previously had NO context at all). Tools gained `durationMinutes` (models are far better at "how long" than at end-time arithmetic) and a `calendar.block` tool; `tasks.create` gained `recurrence`.
- **AI guardrails + token efficiency.** Honesty rules appended to the brief/weekly-review/questions prompts (never infer a trend from a handful of points, never invent activity, say what you'd need to see); `StatsService.summarizeForAi()` returns an explicit sparse-data marker when fewer than 5 of 30 days have activity. `buildContext()` now trims chunks **proportionally** with a per-chunk floor instead of dropping whole domains, and skips summaries that say nothing.
- **Progress, all four asks:** Habit check-ins tile dropped (it duplicated the hero's consistency %); per-habit heatmaps → `ProgressRing` + streak + weekly sparkline (`habitRhythm`); mood is ONE chart with a labelled **1–5 axis** (distribution bars + `moodDistribution` deleted); weekly review renders as bolded bullets (`reviewBullets` + a 2-line `**bold**`-only renderer) and the prompt now *produces* that shape.
- **Verified live** (not just green): recurring task seeded Sat → completed → next instance **Mon**, weekend correctly skipped, parent-linked; weekly event expanded to its next occurrence with `isOccurrence:true`; Progress shows 3 tiles / rings not heatmaps / 1–5 axis / 4 bolded bullets / no horizontal overflow. **Still unverified: end-to-end NL scheduling against a real model** — the DeepSeek key is per-user in `credentials`, not in `.env`, so it needs Riley's own account.
- Green: build 6/6, typecheck 10/10, **286 unit tests**, **e2e 15/15** (incl. a new recurring-task spec + three axe scans).
- **Found: `pnpm lint` has never worked.** `apps/web` runs deprecated `next lint` with no ESLint config, so it prompts interactively and exits 1; CI has no lint step at all. Pre-existing, tracked separately — not a v6 regression.

## v7 — Fitness / strength training (2026-07-25) ✅
The backlog item Riley asked for alongside statistics ("copy whatever fitness app does the best possible UI for tracking workouts") and deferred at the time. Strength training is now a first-class life domain the AI reads.
- **Schema** (migration `20260725014500_fitness`, three new tables, nothing existing touched): `Exercise` (shared catalog with `userId = null` + per-user additions), `Workout` (a session; `endedAt IS NULL` **is** the open one), `WorkoutSet`. Weight is **integer grams**, duration seconds, distance metres — floats drift once you sum volume, and plate maths must stay exact.
- **`modules/fitness/`**: `fitness.service.ts` (start/resume, log set, delete set, finish, history, `lastPerformance`, `summarize`), `exercise-catalog.ts` (32 seeded movements, seeded idempotently on boot by `ExerciseCatalogSeeder`), `fitness.controller.ts`, `fitness.ai.ts`. Domains are now `["tasks","habits","journal","notes","calendar","finance","fitness"]`.
- **Pure training maths lives in `packages/shared/src/dto/fitness-util.ts`** — `workoutVolumeGrams`, `countWorkingSets`, `bestWeightGrams`, `isPersonalRecord`, `describeSet`, `groupSetsByExercise`, `gramsToKg`/`kgToGrams`. Deliberately in **shared, not the API**, so the logger UI and the server compute volume/records/labels from ONE implementation (the no-duplication rule). 17 unit tests.
- **The core loop** (`/fitness`, `FitnessPanel` + `RestTimer`): start → search-first exercise picker (scrolling a long list mid-workout is the slowest possible interaction) → per-exercise blocks showing **what you did last time** → entry row **pre-filled from your last set** (the common case is repeat-or-slightly-beat, so the default action is one tap and zero typing) → auto-starting rest timer (counts UP against a target; overrunning rest is real information) → finish → history. Warm-up is a toggle and is excluded from both volume and records.
- **The AI can start a session but NOT log sets or finish one** — a logged set is a factual claim about what your body did, and a training log you can't trust is worse than none.
- **Four real bugs caught by live verification, none by tests:** (1) the catalog duplicated on every boot — `@@unique([userId,name])` can't dedupe `userId = NULL` rows because Postgres treats NULLs as distinct; (2) `request()` threw on the empty body Nest returns for a `null` handler result, and returning `undefined` then broke TanStack Query — it must be `null`; (3) finishing a workout wrote the finished session into the *active* cache slot, leaving the logger on screen forever; (4) every ascending set badged as a PR, because `lastPerformance` only reads finished sessions so the in-session best has to be folded in by the component. All four are in `docs/GOTCHAS.md`.
- Green: build 6/6, typecheck 10/10, **308 unit tests**, **e2e 16/16**. Verified live end-to-end: 830 kg volume with the warm-up correctly excluded, double-start returns the same session, an empty session is discarded rather than saved as a fake workout, PR badges fire exactly twice for 40→50→45 kg, mobile 390px overflow-free, zero console errors.

### v7.1 — training folded into the rest of the graph (2026-07-25) ✅
Fitness shipped self-contained; this makes it part of the one graph rather than a seventh silo.
- **`/stats` gained `workouts` + `volumeGrams`** (two more raw-SQL aggregates, same local-day bucketing). Volume is bucketed by the SESSION's `endedAt`, not each set's own timestamp, so a workout spanning local midnight stays one training day, and warm-ups are excluded exactly as `FitnessService` excludes them. Only FINISHED sessions count — an open workout is not a training day yet.
- **`/progress`**: a "workouts" hero stat and a **Training volume** card (kg/week sparkline + "N sessions · X kg lifted"), both conditional on there actually being training — same rule as the money card, no zero-filled noise.
- **Weekly review** gains a workouts+volume line, again only when training exists, so the model can correlate training against mood/tasks without being invited to comment on a domain the user doesn't use.
- **Today canvas**: `workout.started` added to `CANVAS_NOISE_TYPES` (the completed row already carries the full summary — "Push day — 2 sets, 830 kg volume"), so a session contributes exactly one card under "Earlier".
- Verified live: 1660 kg across 2 finished sessions (= 830 + 830, warm-up correctly excluded — it would read 1030 otherwise; the unfinished session correctly ignored), Progress hero + card render, workout cards appear on Today with `workout.started` filtered out.
- **Found (pre-existing, NOT fixed — see GOTCHAS):** the Today canvas buckets days by the **browser's** local day while stats bucket by **`User.timezone`**. When those differ, Today and Progress disagree about which day something happened. Affects every domain; fixing it means choosing one clock deliberately.
- Green: build 6/6, typecheck 10/10, **310 unit tests**.

## v8 — day planning: routine editor, free time, "Plan my day" (2026-07-25) ✅
Riley's ask: "make it easy to plan your day and fill gaps", and "when at work it shouldn't say the timeline is open because most people don't put work into their Google Calendars." Shipped across two sessions; the second one live-verified the AI half against the real model and fixed what that turned up.
- **`RoutineBlock.onDate`** (nullable `YYYY-MM-DD`) makes irregular and shift schedules expressible. A dated block replaces the weekly block of the **same kind** for that day only; the new **`off` kind CARVES spans out** instead of adding them (vacation, sick day, swapped shift). Migration `20260725050000_routine_on_date`. Logic is in `routineSegments` (`apps/web/lib/canvas.ts`).
- **`PUT /routine` replaces only the WEEKLY pattern** so dated blocks survive it; per-block `POST /routine/blocks`, `PATCH /routine/blocks/:id`, `DELETE /routine/blocks/:id`.
- **The routine editor** (`components/panels/RoutineEditor.tsx`, in Settings, linked from Today's free-time block). Before it existed `useRoutine` was read-only everywhere, so wrong sleep/work hours were unfixable forever — the actual root of the complaint. Handles all three schedule shapes because this has to work for more people than Riley: (a) fixed weekday hours, (b) varies by day (weekday toggles on a 7-bit mask), (c) shifts/irregular (dated blocks + "I'm off today"). Saves on blur/change — no save button to forget.
- **`buildDayOverview` gained `current`** (the item you are *inside*) as distinct from `next`, fixing a real bug: a sleep event booked 4:11am–11:00am displayed as "Next 4:11" at 4:13am. `NowNext` prefers the live item.
- **`buildDayOverview` gained `gaps`** — free windows still ahead, rendered by `components/canvas/FreeTime.tsx`. EVERY routine block is excluded, not just sleep and work: a meal or gym slot is time you already spoke for. Sub-20-minute slivers are dropped.
- **"Plan my day"** — `OrchestratorService.planDay` + `POST /ai/plan-day`. Deliberately **not** a tool-calling loop: proposals are inert until accepted, every one is matched back to a real task id and dropped otherwise, each must sit inside a window that was actually offered, and no task repeats. Accepting creates a normal calendar event.
- **✅ VERIFIED LIVE against the real DeepSeek model** (it had never been run end-to-end). Two real bugs, neither catchable by unit tests:
  1. **`deepseek-v4-flash` is a REASONING model and its `reasoning_content` is billed against the same `max_tokens` as the answer.** 700 of the shared 800-token budget went to reasoning, so the plan JSON was cut off mid-object, `JSON.parse` failed, and every single call returned "Could not put a plan together just now." `ai_usage` shows `daily_brief` silently hitting the same 800 ceiling twice. Fixed: per-call budgets (`PLAN_RESPONSE_TOKENS` 3000, `NARRATIVE_RESPONSE_TOKENS` 2000 for brief/weekly review), `ChatResult.finishReason` now surfaced by `parseChatCompletion`, and `chatCall` logs a warning when a reply is truncated. `parsePlanReply` moved to `modules/ai/plan-day.util.ts` and now salvages the complete proposals out of a truncated reply.
  2. **A single event wiped out the whole window it sat in.** `openGaps` skipped any Open section containing an event, so accepting one proposal made the rest of the evening vanish, and on a day off — one giant Open section — free time disappeared entirely. Events are now **subtracted** from the window and the remaining slices offered (an event with no end claims 30 min).
- After the fixes, driven live in a real browser: three proposals against real task ids, all inside offered windows, accepting one created the event and it appeared under "Coming up"; a second pass planned *around* that booked event; a day off with one event booked correctly offers 14h 29m.
- **Fixed a racy e2e** (failed deterministically, twice): the recurring-task spec reloaded the page immediately after clicking complete, so the fresh `GET /tasks` raced the server-side spawn of the next instance and fetched a list without it. The DB rows were correct in every run. It now waits for the completion response, asserts the new instance appears **without** a reload (what the user actually sees — measured at ~500ms), then reloads to prove it persisted.
- Green: build 6/6, typecheck 10/10, **334 unit tests**, **e2e 18/18**.

## v8.1 — lint, one clock per user, and learned task durations (2026-07-25) ✅
Two long-tracked debts closed and the first Tier 1 item from `docs/atlas-next-ideas.md` shipped.
- **`pnpm lint` works for the first time.** `apps/web` ran the deprecated `next lint` with no ESLint config, so it prompted interactively and exited 1, and CI had no lint step. ESLint 9 + typescript-eslint were already installed — the config was simply never written. Added `eslint.config.mjs` (flat, whole workspace), `pnpm lint` / `lint:fix`, a per-package script for web, and a **Lint step in CI**. Deliberately **not type-aware** (typecheck already does that across 10 projects, for a fraction of the runtime). `react-hooks` is on for `exhaustive-deps` + `rules-of-hooks`; **v7 of that plugin also ships the React Compiler rules (`set-state-in-effect`, `purity`, `immutability`, `globals`) and those are OFF** — they police a style Atlas hasn't adopted (reading localStorage in a mount effect, reading the clock during render) and enforcing them means rewriting shipped, verified behaviour for a compiler that doesn't run here. Everything it found was **fixed, not silenced**: 3 dead imports, a dead test helper from a deleted feature, a useless assignment in `sw.js`, and 4 unmemoized `?? []` fallbacks that minted a new array identity per render and defeated the `useMemo` they fed (now module-level `NO_TASKS`/`NO_EVENTS`/… constants).
- **The day-bucketing mismatch is FIXED — `User.timezone` is now the one clock.** It defaulted to `'UTC'` and was only ever corrected by finding the Settings toggle, so for every user outside UTC Today (browser-local) and Progress (`AT TIME ZONE User.timezone`) disagreed about which day something happened. Registration now sends the device zone (`browserTimezone()`), and `useTimezoneSync` (mounted in `AppShell`) pushes it whenever it differs — which also repairs existing UTC-stuck accounts and does the right thing on travel (the brief follows you to 7am local). The zone is client-supplied now, so `register` runs it through `safeTz` before it can reach an `AT TIME ZONE` interpolation. **Verified live:** registering in a Toronto browser stores `America/Toronto`; signing into that account from a Tokyo browser re-syncs to `Asia/Tokyo`.
- **Tier 1 #1 — Atlas learns how long your work actually takes.** New `Event.taskId` (migration `20260725180000_event_task_link`, nullable, `ON DELETE SET NULL` so deleting a task never removes time from your calendar) links a reserved block to the task it was for. Accepting a "Plan my day" proposal now records that link, which makes the measurement possible at all: **the block says when you started, `Task.completedAt` says when you stopped.**
  - Maths lives in **`packages/shared/src/dto/duration.ts`** (`durationKey`/`estimateDurations`/`formatDuration`) so the API and the UI compute it from ONE implementation. **Median, not mean** — one task left open over lunch would drag an average somewhere useless. Samples under 2 min (ticking a box you'd already done) or over 8h (you walked away) are discarded, results round to 5 minutes, and **nothing is claimed below 2 samples** — one data point is an anecdote.
  - `TaskDurationService` (read-only, derived, nothing stored — no cache to go stale) + `GET /tasks/durations`. Kept out of `TaskDTO` on purpose: it's the same answer for every row with that title, and the task list stays one query.
  - **`planDay` now feeds the model the user's real medians**, with a prompt rule that their history outranks its own guess. A quiet `usually 1h` chip appears on a task row only once it's measured.
  - **Verified live end-to-end:** two completions of the same task at 50 and 70 minutes produced a learned median of **60 min (2 samples)**, and the planner then allocated **exactly 60 minutes** to that task while an unmeasured one got the 30-minute default. The chip rendered on the measured row only, tooltip "Measured from 2 completed blocks".
  - **No e2e for this one, deliberately:** a deterministic test needs either a stubbed model or backdated `completedAt` values the API rightly won't accept. Covered by unit tests (the maths in `@atlas/shared`, the query shape in `apps/api/test/task-duration.test.ts`) plus the live run above.
- Green: build 6/6, typecheck 10/10, **357 unit tests**, **e2e 18/18**, **lint 0 problems**.

## v8.2 — Tier 1 #2: roll unfinished plans forward, with friction (2026-07-25) ✅
"Overdue lists are where task apps go to die." The cure isn't a better sort order — it's a small **forced, batched decision**: move it to today, or admit it isn't happening.
- **`GET /tasks/slipped`** = open work due before **the user's own midnight** (`localDayStartUtc(User.timezone)` — the one clock from v8.1; a server-midnight boundary would be wrong for hours a day). Work due *later today* is deliberately excluded: the day isn't over, so it hasn't slipped, and asking about it trains the user to dismiss the card unread. Bounded at 25.
- **`POST /tasks/roll-forward`** `{ taskIds, action: 'today'|'drop' }`. Rolling sets `dueAt` to **23:59 local** (midnight would read as overdue again immediately). Dropping sets **`ARCHIVED`** — not deleted (that destroys the signal), not `DONE` (that's a lie that inflates every completion stat on Progress). The write is scoped by `userId` AND open status, so a borrowed id matches nothing. Writes **one timeline row per task** (`task.rolled_forward` / `task.dropped`), not one per batch — "I keep dropping this" is per-task knowledge the AI reads.
- **`SlippedTasks` card** at the top of Today, above Free time: settling the backlog comes before planning on top of it. Checkbox per row (deselect to hold something back), then two buttons — "Move N to today" and "Not doing them". `task.rolled_forward` is added to `CANVAS_NOISE_TYPES` (the task now appears in today's checklist — a card saying so is the same fact twice); `task.dropped` is deliberately NOT noise, since the task vanishes from every list and the feed row is the only trace. "Not now" dismisses **for the day only** (localStorage keyed by day) — a card you can silence forever is a card that stops working.
- **Bug found only by driving it live:** after acting, the deselection set persisted, so the task you deliberately held back came back **still unchecked** — leaving the card showing one task with **both buttons disabled and nothing explaining why**. Deselections now clear on success. Pinned by both a component test and the e2e.
- **Verified live end-to-end:** 3 overdue seeded (work due later today and undated work correctly not offered), deselected one, moved 2 → both due today and gone from the card, held-back one still asked about and re-selectable, dropped it → card empty, task absent from the list, `completedAt` still **null**, and 3 timeline signal rows written with refs set.
- Green: build 6/6, typecheck 10/10, **372 unit tests**, **e2e 19/19**, lint 0.

## NEXT ACTION — pick one (start here)
- **A) Finish the Plaid loose ends (backend already ✅ sandbox-verified).** Left: (1) drive the **in-browser Plaid Link** flow once (Settings → Connect a bank, sandbox creds `user_good`/`pass_good`) to confirm the `usePlaidLink` widget + `/finance` render; (2) **production** cutover for Riley's real Canadian bank — `PLAID_ENV=production` + prod secret (rotate the pasted one) + Plaid production approval + registered `PLAID_REDIRECT_URI`. No code change. (Google Calendar: ✅ verified live 2026-07-19 — remaining edges are token-refresh + delete-propagation, cheap to check.)
- **B) Deploy to the VPS** (unblocked, and the AI brain is now worth using from a phone). Docker Compose + Caddy per `docs/architecture.md`; swap the Neon `DATABASE_URL` for the in-compose `db` service. **Bake the embedding model into the image or pre-warm it** — otherwise the first request on a cold box downloads ~110MB. Watch API-process RAM: the model lives in-process.
- **C) ~~UI~~ DONE — Life-OS UI v2 shipped 2026-07-19 (see the "Life-OS UI v2" section).** The old hardening plan (docs/ui-hardening-plan.md) AND the ground-up redesign (docs/atlas-ui-vision.md) are both complete: ambient AI (⌘K capture/ask/jump command bar, ⌘J chat rail, brief on Home), Home dashboard (spine/focus/rings/pulse), Timeline story view, rich domain views, collapsible sidebar. Remaining UI stretch only: Lighthouse-in-CI budget, Storybook, drag-to-reschedule on the spine, j/k list keyboard nav, a real week-grid calendar. Legacy data-layer facts still true: `networkMode:'always'`; TanStack pauses retries while the tab is unfocused → headless verification must force `window.__focusManager.setFocused(true)` (dev-only handles `__qc`/`__focusManager`).
- **D) Productization debt** (see "Known tracked debt" above): error tracking, Stripe billing, legal pages (privacy/ToS), e2e tests, 2FA/password rules. (Data export + hard delete: DONE.)
- **E) Tier 1 of `docs/atlas-next-ideas.md`** — **#1 (learn how long things take) DONE** (v8.1) and **#2 (batched roll-forward) DONE** (v8.2). Remaining: **#3 energy-aware placement** (learn *when* in the day demanding work actually gets finished — the `task.rolled_forward`/`task.dropped` rows and the duration samples are now both real inputs for this — and have the planner prefer those windows while protecting the low-energy one), then **#4 one-tap "running 30 min late"**. That file also lists what NOT to build, which matters just as much.

Then Phase 3 (finance connector: SimpleFIN/Plaid), Phase 4 (proactive nudges). See `docs/roadmap.md` + the plan file.

## GOTCHAS — already solved, do NOT rediscover
- **pnpm ignores dependency build scripts** (`ERR_PNPM_IGNORED_BUILDS`, Prisma engine missing). Fix: `pnpm-workspace.yaml` has an `allowBuilds:` map (pnpm 11 key) → `'@prisma/client': true`, `'@prisma/engines': true`, `prisma: true`. Already set. If a new dep needs a build script, add it there.
- **Node globals need per-package `@types/node`** (pnpm strict, no hoist). Missing → `TS2304: Cannot find name 'fetch'/'AbortSignal'/'process'`. Any package touching fetch/Buffer/process/crypto must list `@types/node` in its own devDependencies. Already added to db, ai, connectors, api.
- **Prisma `Json` columns reject `Record<string,unknown>`/`unknown`.** Cast at the prisma call to `Prisma.InputJsonValue` (`import type { Prisma } from '@atlas/db'`). Never put a `Date` in a JSON payload — use `.toISOString()`. Already handled in timeline.service, connectors.service, tasks.service.
- **NestJS must be built with `tsc`, never `tsx`/esbuild.** Nest DI relies on `emitDecoratorMetadata` (`design:paramtypes`), which esbuild/tsx do NOT emit → broken DI. So: ESM + tsc. Dev uses `concurrently` (tsc --watch + node --watch). Do not "optimize" api to tsx.
- **ESM discipline:** every package.json has `"type": "module"`; relative imports in `.ts` use `.js` extensions; packages export built `dist` (not src); turbo `^build` builds deps first. tsconfig.base is NodeNext.
- **PowerShell noise:** native commands' stderr gets wrapped as red `NativeCommandError` / a `pnpm.ps1` error block EVEN ON SUCCESS. Judge success by the actual ✔/output, not the red text. Don't use `2>&1` on native exes here.
- Working dir at launch is usually `C:\`; the project is at `C:\Users\riley\atlas` — `cd` there first.
- **Docker Desktop crashes on boot at "Inference manager"** (its Model Runner/AI feature), then all `docker` calls hang. Fix: with Docker stopped, set `"EnableDockerAI": false` in `%APPDATA%\Docker\settings-store.json` and relaunch. Full detail in `docs/GOTCHAS.md`. Already applied.
- **DeepSeek's function-calling API rejects dotted tool names** (`Invalid 'tools[0].function.name': string does not match pattern '^[a-zA-Z0-9_-]+$'`). Atlas's tool specs use dots (`tasks.create`) for readability everywhere else, so `packages/ai/src/tools.ts` maps to `tasks__create` only at the provider boundary and back on the way in (`toWireToolName`/`fromWireToolName`). If you add a new provider, don't assume dotted names are safe — check its function-name pattern first.
- **Atlas uses DeepSeek direct, NOT OpenRouter.** `.env`'s `OPENROUTER_API_KEY` was a stale placeholder (always blank) — Riley bought DeepSeek platform credits, so chat uses `DeepSeekConnector` against `api.deepseek.com` with a `deepseek` credential (`POST /ai/connect/deepseek`). Model ids have no `deepseek/` prefix — that's the OpenRouter style. `OpenRouterConnector` was **deleted**: DeepSeek has no embeddings API (verified 404), and rather than make Riley pay a second provider for vectors, embeddings run locally (`LocalEmbedder`). Don't reintroduce OpenRouter without a reason.
- **Local embeddings need two pnpm workarounds** (`pnpm-workspace.yaml`). (1) `allowBuilds` must include `onnxruntime-node` + `protobufjs` (native binary + install-time codegen), the usual `ERR_PNPM_IGNORED_BUILDS` trap. (2) `@huggingface/transformers` imports `onnxruntime-common` **without declaring it** — a phantom dep that works under hoisted node_modules but throws `ERR_MODULE_NOT_FOUND` under pnpm's strict linking, because Node resolves it from inside the transformers package's own store dir (adding it to `packages/ai` does NOT help). Fixed with a `packageExtensions` entry declaring it on the package's behalf.
- **`LocalEmbedder`'s model width is coupled to the DB.** `Xenova/bge-base-en-v1.5` → 768 dims → `embeddings.embedding vector(768)`. Swapping to a 384-dim model (e.g. all-MiniLM-L6-v2) requires a column migration; the embedder throws `EmbeddingDimensionError` instead of writing a bad vector. The model downloads (~110MB) on first use — pre-warm or bake it into the Docker image so the first VPS request isn't slow.
- **Use a concrete DeepSeek model id (`deepseek-v4-flash`), never the `deepseek-chat` alias.** The alias resolves server-side (currently → `deepseek-v4-flash`) and the API echoes the **resolved** id back, which is what `CostGuard.record()` writes to `ai_usage`. If the resolved id isn't in `MODEL_RATES`, every row silently prices at the FALLBACK rate — this actually happened (real cost ~47 micro-USD/chat was logged as 1020). Riley's account only exposes `deepseek-v4-flash` + `deepseek-v4-pro`; **legacy `deepseek-chat`/`deepseek-reasoner` are removed 2026-07-24.** When adding a model, add its rate at the same time.
- **DeepSeek prefix-cache hits are ~98% cheaper and Atlas hits them constantly** (~90-95% of prompt tokens, since every call re-sends the same context block). The API reports `prompt_cache_hit_tokens`; `parseChatCompletion` reads it into `ChatUsage.cachedPromptTokens` and `estimateCostMicros` bills it at `cachedInputMicros`. Ignoring it overstates spend ~3.5x. Fractional rates also need `toFixed(6)` before `Math.ceil`, or float noise adds a phantom micro.
- **`pnpm build` while dev servers run breaks BOTH dev processes on Windows:** `next build` clobbers `apps/web/.next` under the running dev server (→ ENOENT vendor-chunk 500s, page stuck on "Loading…"), and `prisma generate` can't rename the query-engine DLL the running API holds (`EPERM ... query_engine-windows.dll.node`). Stop dev servers before `pnpm build`, or restart them after.
- **Claude browser-pane screenshots don't work against the Atlas app** (30s timeout every time; clicks/read_page/JS fine). Use `read_page`/`get_page_text` for verification and the Claude-in-Chrome tools when an actual screenshot is needed. Details in `docs/GOTCHAS.md`.
- **e2e registration is throttled (5/min/IP) — don't register a fresh user per test.** More than ~5 quick `register()` calls across specs starts 429ing and tests fail with confusing "element not found" (the sign-in never happened). Pattern used in `apps/web/e2e/life-os.spec.ts`: register ONCE in `beforeAll` (with `browser.newPage({ storageState: undefined })` — `test.use({storageState})` leaks into `browser.newPage()` and would try to read the not-yet-written file), save `storageState`, `test.use` it, and have each test `goto()` + wait for `.sidebar-user-name` before sending hotkeys (the ⌘K/⌘J listeners attach only after the authed shell mounts).
- **Browser-pane JS drive of the Atlas app: screenshots AND read_page are broken (0x0 viewport), and synthetic `KeyboardEvent`s don't reach the app's window listener.** Verify keyboard shortcuts through Playwright (trusted events), and verify pages via `get_page_text`/`javascript_tool` DOM queries. Clicks via `element.click()` in `javascript_tool` work fine.
- **Neon `migrate dev` wants a DESTRUCTIVE RESET (drift from a Neon-managed extension).** Neon auto-adds `pg_session_jwt`, which isn't in the migration history, so `prisma migrate dev` reports drift and offers only `migrate reset` (drops the DB — all data lost). **Never reset.** To apply an additive migration: `prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel ./prisma/schema.prisma --script` → inspect (must be additive, no `DROP EXTENSION`) → write it as `prisma/migrations/<ts>_<name>/migration.sql` → `prisma migrate deploy` (deploy ignores drift, no reset) → `prisma generate`. This is how `20260719160000_finance_plaid_fields` was applied.
- **Plaid amount sign is INVERTED vs Atlas.** Plaid `amount` is **positive = money out** (a debit); Atlas `transaction.amountMinor` is **negative = money out**. `plaidAmountToMinor(amount) = Math.round(-amount*100)`. Get it backwards and every expense reads as income. Unit-tested both directions (`packages/connectors/test/plaid.test.ts`). Currency = `iso_currency_code ?? unofficial_currency_code ?? 'USD'`.
- **Plaid uses raw `fetch`, no `plaid` SDK** (same call as GoogleCalendarConnector) — Plaid is plain JSON REST. Only the web needs a dep (`react-plaid-link` — Plaid Link is a hosted iframe handshake that can't be hand-rolled). `PLAID_*` env is all optional: unset ⇒ connector unregistered, Settings shows "unavailable", Atlas runs fine. `sandboxCreatePublicToken` lets you verify the full exchange→sync path with no human — use it for live sandbox testing.
- **`deepseek-v4-flash` is a REASONING model and its `reasoning_content` eats the same `max_tokens` as the answer.** 700 of an 800-token budget went to thinking on a real plan-day call, truncating the JSON mid-object (`finish_reason: "length"`, HTTP 200, no error anywhere) so the feature failed 100% of the time. Budget for reasoning + answer, not the answer alone: `chatCall` takes a per-call `maxTokens`, and anything whose output must be complete to be usable gets a big one. Check `ChatResult.finishReason`; find past damage by looking for `ai_usage` rows sitting exactly on the cap.
- **⚠️ Prompt message ORDER is load-bearing for cost — volatile content goes LAST.** It's a *prefix* cache, so the first differing token kills the discount for everything after it. Putting per-message semantic recall in the system prompt measured **0% cache hit**; the same text appended to the final user message measured **~92%** (verified in `ai_usage`: 182µ¢ → 135µ¢ for the same chat). Required order: **static instructions → module context → history → user message (+ per-message extras)**. This regresses silently — no error, just a ~3.5x bill. If you add anything per-request to a prompt, put it at the end and re-measure.
```
```
