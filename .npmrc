# Hoist nothing by default (pnpm strictness) but allow common build tools.
# Keeps the dependency graph honest so future modules can't rely on phantom deps.
auto-install-peers=true
strict-peer-dependencies=false
