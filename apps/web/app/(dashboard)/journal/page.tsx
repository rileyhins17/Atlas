import { WritingPanel } from '@/components/panels/WritingPanel';

/** Journal and Notes are one surface now. The route stays so links still work. */
export default function JournalPage() {
  return <WritingPanel />;
}
